{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .images {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1em;
    }
    .card {
        max-width: 50%;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
        overflow: hidden;
    }
    .card img {
        width: 100%;
        height: auto;
        display: block;
    }
    .card-body {
        padding: 1em;
        text-align: center;
    }
</style>

<div class="images">
    <h1>Mes Loisirs et Passions</h1>
    <p>Voici quelques photos qui illustrent mes loisirs et projets !</p>
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Jeux vidéos</h5>
            <img src="{{asset('image/jeux.jpg')}}">
        </div>
        <div class="card-body">
            <h5 class="card-title">mangas</h5>
            <img src="{{asset('image/mangas.jpg')}}">
        </div>
        <div class="card-body">
            <h5 class="card-title">Etre avec mes amiss</h5>
            <img src="{{asset('image/ami.jpg')}}">
        </div>
    </div>
</div>
{% endblock %}